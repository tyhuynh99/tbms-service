# application port
server:
  port: 8888
  error:
    include-stacktrace: never
  servlet:
    context-path: /tbms_service

# spring setting
spring:
  datasource:
    url: jdbc:postgresql://58.84.1.74:5432/tbms
    username: admin
    password: 123
    sql-script-encoding: UTF-8
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true

# firebase
firebase:
  admin-key: tbms-a082d-firebase-adminsdk-43tdy-6fc21f54ab.json
  bucket-name: tbms-a082d.appspot.com
  preview-url: https://firebasestorage.googleapis.com/v0/b/tbms-a082d.appspot.com/o/%s?alt=media